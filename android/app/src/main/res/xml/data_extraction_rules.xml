<?xml version="1.0" encoding="utf-8"?>
<!--
  Project XP — règles de sauvegarde / transfert Android 12+.

  Même politique que backup_rules.xml : aucune donnée interne de Project XP
  n'est copiée automatiquement vers le cloud ou vers un nouvel appareil tant
  que les données sensibles et les préférences ordinaires ne sont pas séparées.
-->
<data-extraction-rules>
    <cloud-backup disableIfNoEncryptionCapabilities="true">
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>

    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
